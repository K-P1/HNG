# Environment configuration for Reflective Assistant
DATABASE_URL=sqlite+aiosqlite:///./dev.db
TEST_DATABASE_URL=sqlite+aiosqlite:///./test.db

# Optional: API keys for LLM providers
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# App settings
ENV=development
LLM_PROVIDER=groq
DEBUG=false

# A2A agent name (used to build the route at /a2a/agent/<A2A_AGENT_NAME>)
A2A_AGENT_NAME=Raven

# Async configuration for A2A handling
# A2A_ASYNC_ENABLED controls how /a2a/agent handles preview vs final responses:
# - Unset (leave blank) → use request.configuration.blocking
# - false / 0 / no → force synchronous (single final TaskResult; ignore push_url)
# - true / 1 / yes → prefer async when a push_url is provided (unless blocking: true)
# Note: Reminder scheduler only runs when this is set to true
A2A_ASYNC_ENABLED=

# Reminder settings
REMINDER_CHECK_INTERVAL_MINUTES=1
REMINDER_ADVANCE_HOURS=24,1
REMINDER_QUIET_HOURS_START=22
REMINDER_QUIET_HOURS_END=8
REMINDER_MAX_OVERDUE_REMINDERS=5
REMINDER_OVERDUE_INTERVAL_HOURS=24